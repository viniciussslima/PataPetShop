const formatDate = require("./formatDate");
const getDate = require("./getDate");
const getHour = require("./getHour");
const log = require("./log");

module.exports = {
  formatDate,
  getDate,
  getHour,
  log,
};
